/*
1. Declare variables called 'country', 'continent', and 'population' and assign their values according to your own country (population in millions)

2. Log their values to the console
*/

const country = "United States";
const continent = "North America";
const population = 329_500_000;

console.log(
  `${country} is located in ${continent} and has a population of ${population} people`
);

/*
1. Create a variable that is either a let or a const and add a variable name, then assign a value 

2. Create a variable that has a string
*/

const lengthOfBootCamp = 10; //weeks

const favoriteQuote = "Trust the process.";
